spring:
  datasource:
    url: jdbc:h2:mem:feauxauth-test;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration
    clean-disabled: false
  thymeleaf:
    cache: false

feauxauth:
  issuer: http://localhost:8080
  admin:
    username: admin
    password: feauxauth
